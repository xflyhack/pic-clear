name: Build Pipe GUI EXE

on:
  push:
    branches: [main, master]
    tags: ['v*']
  workflow_dispatch:


permissions:
  contents: write   # softprops/action-gh-release 需要写权限来创建/更新 Release

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install runtime deps
        run: |
          python -m pip install --upgrade pip
          pip install cryptography>=41.0
          pip install pystray>=0.19.5 Pillow>=10.0.0 keyboard>=0.13.5
          pip install pyinstaller pyarmor

      - name: Prepare build folder
        run: |
          mkdir build_pipe_gui
          copy pipeline.py build_pipe_gui\
          copy pipe_gui.py build_pipe_gui\
          copy licensing.py build_pipe_gui\
          copy otp_utils.py build_pipe_gui\
        shell: cmd

      - name: Obfuscate with PyArmor
        run: |
          cd build_pipe_gui
          REM 拆分 pyarmor gen 调用绕开 trial 版单次调用配额限制
          pyarmor gen --output dist_obf pipeline.py
          pyarmor gen --output dist_obf pipe_gui.py
          pyarmor gen --output dist_obf licensing.py
          pyarmor gen --output dist_obf otp_utils.py
          dir dist_obf
          copy /Y dist_obf\pipeline.py pipeline.py
          copy /Y dist_obf\pipe_gui.py pipe_gui.py
          copy /Y dist_obf\licensing.py licensing.py
          copy /Y dist_obf\otp_utils.py otp_utils.py
          cd ..
        shell: cmd

      - name: Build single-file exe (windowed，无控制台黑窗)
        run: |
          cd build_pipe_gui
          pyinstaller --onefile --windowed --name pipe_gui ^
            --icon "..\icon.ico" ^
            --add-data "..\icon.ico;." ^
            --add-data "..\icon.png;." ^
            --add-data "dist_obf\pyarmor_runtime_000000;pyarmor_runtime_000000" ^
            --hidden-import json ^
            --hidden-import base64 ^
            --hidden-import hashlib ^
            --hidden-import socket ^
            --hidden-import subprocess ^
            --hidden-import datetime ^
            --hidden-import signal ^
            --hidden-import ctypes ^
            --hidden-import ctypes.wintypes ^
            --hidden-import concurrent ^
            --hidden-import concurrent.futures ^
            --hidden-import concurrent.futures.thread ^
            --hidden-import cryptography ^
            --hidden-import cryptography.hazmat.primitives.asymmetric.padding ^
            --hidden-import cryptography.hazmat.primitives.hashes ^
            --hidden-import cryptography.hazmat.primitives.serialization ^
            --hidden-import licensing ^
            --hidden-import otp_utils ^
            --hidden-import pipeline ^
            --hidden-import tkinter ^
            --hidden-import tkinter.ttk ^
            --hidden-import tkinter.filedialog ^
            --hidden-import tkinter.messagebox ^
            --hidden-import pystray ^
            --hidden-import pystray._win32 ^
            --hidden-import PIL ^
            --hidden-import PIL.Image ^
            --hidden-import PIL.ImageDraw ^
            --hidden-import keyboard ^
            pipe_gui.py
          cd ..
        shell: cmd

      - name: Show exe size
        run: dir build_pipe_gui\dist

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pipe-gui-windows-exe
          path: build_pipe_gui/dist/pipe_gui.exe
          if-no-files-found: error

      - name: Attach exe to GitHub Release (only on tag push)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: build_pipe_gui/dist/pipe_gui.exe
          fail_on_unmatched_files: true
